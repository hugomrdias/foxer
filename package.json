{
  "name": "foxer-repo",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "Hugo Dias <hugomrdias@gmail.com>",
  "workspaces": [
    "apps/*",
    "packages/*",
    "examples/*"
  ],
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ready": "vp run build -r && vp check --fix",
    "check": "vp run --cache -r check",
    "build": "vp run --cache -r build",
    "clean": "rm -rf node_modules pnpm-lock.yaml packages/*/{dist,node_modules,.turbo} apps/*/{dist,node_modules,.turbo} examples/*/{dist,node_modules,.turbo}",
    "prepare": "vp config"
  },
  "devDependencies": {
    "turbo": "^2.8.17",
    "vite-plus": "0.1.11"
  },
  "packageManager": "pnpm@10.30.1"
}
