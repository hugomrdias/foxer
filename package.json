{
  "name": "foxer-repo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "pnpm -r --if-present run lint",
    "build": "pnpm -r --if-present run build",
    "lint:fix": "biome check --write .",
    "clean": "rm -rf node_modules pnpm-lock.yaml packages/*/{dist,node_modules} apps/*/{dist,node_modules}"
  },
  "keywords": [],
  "author": "Hugo Dias <hugomrdias@gmail.com>",
  "license": "MIT",
  "packageManager": "pnpm@10.30.1",
  "devDependencies": {
    "@biomejs/biome": "^2.4.5",
    "@hugomrdias/configs": "^1.1.2"
  }
}
